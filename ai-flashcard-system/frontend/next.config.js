/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // App Router is on by default in Next 15; nothing to enable here.
  },
};

module.exports = nextConfig;
